#include <iostream>

using namespace std;

int main() {
	setlocale(LC_ALL, "Russian");
	double A, B;
	cout << "A = ";
	cin >> A;
	cout << "B = ";
	cin >> B;
	for (; A != B;)
	{
		if (A > B)
			A -= B;
		else B -= A;
	}
	cout << A << endl;
	return 0;
}
