#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    setlocale(LC_ALL, "Russian");
    int x;
    cout << "Введите размер файла в байтах: ";
    cin >> x;
    x /= 1024;
        cout << "Полное количество занимаемых килобайт: " << x << endl;
    system("pause");
    return 0;
}
