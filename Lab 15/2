#include <iostream>

using namespace std;

int Sign (double X)
{
	if (X > 0)
		return 1;
	else if (X == 0)
		return 0;
	else
		return -1;
}

int main() {
	setlocale(LC_ALL, "Russian");
	double A, B;
	cout << "A = ";
	cin >> A;
	cout << "B = ";
	cin >> B;
	cout << Sign(A) + Sign(B);
	return 0;
}
