#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    setlocale(LC_ALL, "Russian");
    int a;
    bool bo;
    cout << "A = ";
    cin >> a;
    bo = ((a % 2 == 0) & (a < 100) & (a > 9));
    cout << bo << endl;
    system("pause");
    return 0;
}
