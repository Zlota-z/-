#include <iostream>
#include <cmath>

using namespace std;

int main() {
	setlocale(LC_ALL, "Russian");
	double N, A, s = 1;
	cout << "N = ";
	cin >> N;
	cout << "A = ";
	cin >> A;
	for (double Ap = A; Ap <= pow (A, N); Ap *= A)
	{
		s += Ap;
	}
	cout << s << endl;
	return 0;
}
